---
import { site } from "@datas/site";
import clsx from "clsx";
import XIcon from "@icons/lucide/x.svg";
import ShareIcon from "@icons/lucide/share2.svg";
import Users from "@icons/lucide/users.svg";

// Type & Interfaces
interface Props {
    class?: string;
}
// Vars
const { class: customClass, ...rest } = Astro.props;
---

<button
    class={clsx("transition-all btn btn-soft btn-primary btn-circle", customClass)}
    {...rest}
    onclick="modalCopy.showModal()"
>
    <ShareIcon />
</button>

<dialog id="modalCopy" class="modal">
    <div class="modal-box w-full max-w-xs">
        <h3 class="text-xl font-bold text-center mb-6">
            <Users class="inline-flex self-center me-2" /> Ajak temanmu kesini
        </h3>
        <div class="flex flex-col gap-2">
            {
                site.shareOptions.map((item) => {
                    return (
                        <button class="btn btn-soft btn-primary justify-start" data-action={item.action}>
                            <item.icon />
                            <span>{item.label}</span>
                        </button>
                    );
                })
            }
        </div>
        <div class="modal-action justify-center">
            <form method="dialog">
                <button class="btn btn-circle btn-secondary btn-soft"><XIcon /></button>
            </form>
        </div>
    </div>
    <form method="dialog" class="modal-backdrop">
        <button>close</button>
    </form>
</dialog>
